@model IEnumerable<SlideShowViewModel>

<div class="table-responsive">
    <table id="all-roles-list" class="table table-striped table-hover table-bordered">
        <thead class="thead-light">
            <tr>
                <th class="text-center">نام</th>
                <th class="text-center w-25">عملیات</th>
            </tr>
        </thead>
        <tbody>
            @if (!Model.Any())
            {
                <tr>
                    <td colspan="2" class="text-center">هیچ دیتایی یافت نشد</td>
                </tr>
            }
            else
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td class="text-center">@item.Title</td>
                        <td class="text-center">
                            <security-trimming asp-area="" asp-controller="SlideShow" asp-action="RenderEdit">
                                <a id="btnEdit-@item.Id" data-edit-id="@item.Id" class="btn btn-sm btn-primary" asp-controller="SlideShow" asp-action="RenderEdit" asp-route-id="@item.Id">ویرایش</a>
                            </security-trimming>
                            <security-trimming asp-area="" asp-controller="SlideShow" asp-action="RenderDelete">
                                <a href="#" id="btnDelete-@item.Id" data-delete-id="@item.Id" class="btn btn-sm btn-danger">حذف</a>
                            </security-trimming>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>