@model IEnumerable<PageViewViewModel>

@foreach (var item in Model)
{
    <li class="list-group-item d-flex justify-content-between align-items-center">
        @item.PageUrl
        <span class="badge">@item.ViewCount</span>
    </li>
}