@model IEnumerable<UserBrowserViewModel>

@foreach (var item in Model)
{
    <li class="list-group-item d-flex justify-content-between align-items-center">
        @item.Name
        <span class="badge badge-primary badge-pill">@item.ViewCount</span>
    </li>
}