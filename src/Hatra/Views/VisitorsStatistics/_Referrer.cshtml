@model IEnumerable<ReferrerViewModel>

@foreach (var item in Model)
{
    <li class="list-group-item d-flex justify-content-between align-items-center">
        <span class="badge badge-secondary badge-pill">@item.ViewCount</span>
        @item.Referrer
    </li>
}